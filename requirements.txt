# Shared / deploy-safe. Render installs this file if the dashboard
# still uses `pip install -r requirements.txt`.
# Local training extras: pip install -r requirements-ml.txt
numpy>=2.0
xarray>=2024.10
netCDF4>=1.6
h5netcdf>=1.4
scipy>=1.11
matplotlib>=3.8
tqdm>=4.65
flask>=3.0
gunicorn>=22.0
